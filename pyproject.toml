[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
packages = "oceanalysis"

[project]
name = "oceanalysis"
version = "0.0.2"
authors = [
    { name = "Reyk Boerner", email = "r.borner@uu.nl" },
    { name = "Lucas Esclapez", email = "l.p.t.esclapez@uu.nl" },
]
dependencies = [
  "xarray",
  "gsw_xarray",
  "numexpr",
]

[tool.setuptools.packages.find]
include = ["oceanalysis*"]